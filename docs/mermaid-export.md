# Mermaid Export

Mermaid export emits deterministic class diagram source. Compact mode includes entities and relationships. Detailed mode adds properties. Action summaries can be included optionally.

Mermaid is not required by OpenOntologyLite.
