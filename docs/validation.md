# Validation

Validation has structural and semantic layers. Structural validation rejects malformed files, unsupported extensions, unknown fields, and invalid field types during parsing. Semantic validation checks identifiers, references, constraints, permissions, preconditions, limits, and schema version support.

Validation results include severity, code, message, logical path, suggestion, and context when applicable.
